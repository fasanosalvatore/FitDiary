server:
  error:
    include-message: always

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
  datasource:
    url: jdbc:h2:mem:mydb;DB_CLOSE_DELAY=-1
    username: sa
    driver-class-name: org.h2.Driver